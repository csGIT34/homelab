---
apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: argocd-ingress
  namespace: argocd
spec:
  entryPoints:
    - websecure
  routes:
    - match: HostSNI(`argocd.homelab.local`)
      services:
        - name: argocd-server
          port: 443
  tls:
    passthrough: true
